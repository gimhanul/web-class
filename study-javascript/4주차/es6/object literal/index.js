function addressMaker(city, state) {
    // const newAdress = {newCity: city, newState: state};
    const newAdress = {city, state};
    console.log(newAdress);
}

addressMaker("Pyeongtaek", "Dongsakdong");